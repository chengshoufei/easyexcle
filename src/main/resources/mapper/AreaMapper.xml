<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo1.mapper.AreaMapper">
  <select id="getAreas" resultType="com.example.demo1.entity.TArea">
        select * from t_area where pid=#{pid};
  </select>


    <select id="getArea" resultType="com.example.demo1.entity.TArea">
        select * from t_area where pid=#{pid};
    </select>
</mapper>
